<%= form_with(model: event_page, local: true) do |form| %>
  <% if event_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_page.errors.count, "error") %> prohibited this event_page from being saved:</h2>

      <ul>
      <% event_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :event_page_title %>
  </div>

  <div class="field">
    <%= form.label :host %>
    <%= form.text_field :host, id: :event_page_host %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
